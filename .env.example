# Database
POSTGRES_PASSWORD=changeme

# Backend
JWT_SECRET=change-this-to-a-random-32-char-secret
RESEND_API_KEY=re_xxxxxxxxxxxx
EMAIL_FROM=noreply@yourdomain.com

# Frontend build arg (leave empty to use relative /api proxy)
# Set this to the backend's public URL when deploying separately on CapRover:
# VITE_API_URL=https://oath-backend.your-caprover.com/api
VITE_API_URL=
